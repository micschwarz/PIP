<?php

$config = [
    'base_url'           => '', // Base URL including trailing slash (e.g. http://localhost/)
    'default_controller' => 'Main', // Controller used for errors (e.g. 404, 500 etc)
    'default_action'     => 'index', // Controller used for errors (e.g. 404, 500 etc)
    'error_controller'   => 'ErrorHandler', // Controller used for errors (e.g. 404, 500 etc)
    'db_host'            => '', // Database host (e.g. localhost)
    'db_name'            => '', // Database name
    'db_username'        => '', // Database username
    'db_password'        => '', // Database password
];
